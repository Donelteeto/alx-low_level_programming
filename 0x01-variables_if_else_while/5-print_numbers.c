#include <stdio.h>

/**
 * main - program that prints number to base 10
 *
 * Return: 0 (always)
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
